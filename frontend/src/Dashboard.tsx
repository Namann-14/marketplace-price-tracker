// Legacy Dashboard.tsx — kept for backwards compat.
// The app now routes through App.tsx → DashboardPage / ProductsPage / ProductDetailPage.
// This file is no longer the main entry point.
export default function Dashboard() {
  return null;
}
